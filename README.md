
  # HelpCenter

  This is a code bundle for HelpCenter. The original project is available at https://www.figma.com/design/XaAfXHMg0LWJ8dNBoK423D/HelpCenter.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  